import 'package:flutter/material.dart';

const kBold = TextStyle(
  fontWeight: FontWeight.bold,
);

const kUserMessageDecoration = const BoxDecoration(
    borderRadius: const BorderRadius.all(
      const Radius.circular(10.0),
    ),
    color: Colors.lime);
